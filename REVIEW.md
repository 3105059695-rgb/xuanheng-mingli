# 玄衡 2.8.1 检查与修复记录

检查日期：2026-09-14。输入：作者提供的「方木-玄衡-v2.8-交付包.zip」。

本记录描述本次实际执行的检查，不沿用原包中“已核验”的表述作为本次验证结果。

## 完整性

- 原交付压缩包共 77 个文件，解压内容合计 745,477 字节；解压前检查了路径越界、重复路径和符号链接。
- 原包清单中 76 个文件的字节数和 SHA-256 全部匹配；清单本身不在其校验列表内。
- 原包的 `mingli/` 与内嵌 `mingli-v2.8-skill.zip` 的 38 个文件逐一一致。
- 原始压缩包 SHA-256：`55bb8d04e86bf6546563a2c146b35794ea0d109cb49d64cdf97aafc09afc6ac8`。
- 修复在独立副本中完成；原交付压缩包与解压原件保留。

## 发现与修复

| 问题 | 复现情况 | 本次处理 |
| --- | --- | --- |
| Windows 干净环境缺少 IANA 时区数据 | 仅安装原 `requirements.txt` 后，时区测试出现 `ModuleNotFoundError: tzdata` 及换算失败 | 加入 `tzdata==2026.4`；保留系统数据优先和实际来源记录 |
| 重定向输出时卦象符号无法用 GBK 编码 | `liuyao.py` 输出乾卦符号出现 `UnicodeEncodeError`；模拟起卦 CLI 同类失败 | 7 个 CLI 入口统一 UTF-8 标准输出/错误；原有子进程测试明确以 UTF-8 读取 |
| 安装说明未覆盖 Windows | 只提供 `.venv/bin/python` 和 shell 反斜杠续行 | 补充 PowerShell 安装、执行、测试命令，不要求修改执行策略 |
| 可复用规则夹带先前对话身份 | 报告规范开头将八条规则描述成“用户明确指定” | 改为 Skill 默认规范，服从当前用户的格式和详略要求 |
| 缺少仓库入口与本地数据排除项 | 原包为交付归档，未提供仓库首页 | 增加 README、忽略规则及本记录；版本更新为 2.8.1 |

Windows 时区依赖的处理依据亦见 [Python 官方 zoneinfo 数据源说明](https://docs.python.org/3/library/zoneinfo.html#data-sources)。依赖版本已从 PyPI 获取并实际安装。本次没有更换 `lunar-python==1.4.8`，也没有改写传统术法公式或既有参考规则。

## 实际验证

环境：Windows，Python 3.12 独立虚拟环境，`lunar-python==1.4.8`、`tzdata==2026.4`。

| 检查 | 结果 |
| --- | --- |
| 原版自动测试 | 执行 151 项；报告 3 个失败、16 个错误，均出现在本次环境的兼容性问题上。失败数包含子测试，不能据此直接推算“通过项数” |
| 修复版自动测试 | 154 项全部通过，无跳过 |
| 新增兼容性回归 | ASCII 输出环境下保存中文及卦象 JSON、无系统时区数据时使用 tzdata、错误输入输出可读 UTF-8 JSON |
| Skill 创建工具结构校验 | `quick_validate.py` 通过 |
| Python 依赖一致性 | `pip check` 通过 |
| 本地文件引用 | 原技能 108 处全部可解析；发布版 Markdown 与首页 HTML 共 131 处，文件目标全部存在 |
| 首页排版 | 使用 GitHub Markdown API 转换，在本地宽屏与 390px 窄屏预览；图片正常加载，无全页横向溢出。预览样式模拟 GitHub，未冒充线上页面截图 |

复现测试（在 `mingli/` 内）：

```powershell
.venv/Scripts/python.exe -m unittest discover -s tests -v
.venv/Scripts/python.exe -m pip check
```

## 静态检查与发布范围

逐个阅读了原有 7 个计算脚本，并检查了测试中的进程调用。未发现脚本发起网络请求、执行 shell 拼接命令、删除文件或读取凭据的代码；命令行双人关系工具会读取显式传入的 JSON 文件，时区工具会读取时区数据。这是源码检查结果，不是沙箱运行监测或第三方依赖供应链审计。

对发布文本做了常见密钥格式、本机绝对路径和越权指令模式扫描，未发现实际密钥或制作电脑路径。教学/合成示例与复核空表保留；本次没有采集真实客户资料。

本次发布内容共 45 个文件，为 `mingli/` 技能本体（含参考资料、教学样例、8 个脚本文件与 8 个测试文件）、仓库说明、首页 SVG 视觉素材和检查记录。8 个脚本文件中包含 7 个计算入口及 1 个共用输出辅助模块。原交付包的重复压缩包、旧版本交付报告、制作日志、虚拟环境和缓存留在本地。当前未新增项目许可证。

## 尚未验证

- 未逐页重校全部古籍、引文和外部链接，未独立证明所有传统规则解释。
- 未验证预测准确率、真实客户体验、付款能力或真人专业认可。
- 未在 macOS/Linux 或所有宣称支持的 Python 版本实际运行。
- 原有的太阳时完整排盘、后续每步大运精确交接、婚期与适配评分限制仍然存在。

上述范围不影响本次确认的 Windows 修复结果，但不应将测试通过表述为“算命准确”或“所有平台和规则均已认证”。
